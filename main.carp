(system-include "Arduino.h")

(load "config.carp")

(load "src/serial.carp")

(sig setup (Fn [] ()))
(defn setup []
  (do
   (Serial.init 74880l)
   (let [a-map {1 2}]
     (Serial.println "test"))))

(sig loop (Fn [] ()))
(defn loop [] ())

